<div class="flickr">
  <ul>
    <% photos.each do |row| %>
    <li>
      <% row.each do |pic| %>
        <%= link_to(image_tag(make_flickr_url(pic, 's'), :class => 'flickr_photo'), make_flickr_url(pic, 'h')) %>
      <% end %>
    </li>
  <% end %>
</ul>
</div>
